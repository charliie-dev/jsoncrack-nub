# Loaded only by `next dev`, at higher precedence than .env.
#
# Development gets a higher node ceiling than production on purpose: contributors need
# to exercise larger documents (CONTRIBUTING asks for it), and a dev machine can absorb
# a slow layout that a visitor’s tab cannot. Upstream shipped this same 2000 here.
NEXT_PUBLIC_NODE_LIMIT=2000
